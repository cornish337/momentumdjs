# Nodemailer Configuration for Contact Form
EMAIL_SERVICE= # e.g., 'gmail' (optional, often inferred by host)
EMAIL_HOST=    # e.g., 'smtp.gmail.com' (required if service not specific enough)
EMAIL_PORT=    # e.g., 587 (for TLS) or 465 (for SSL)
EMAIL_SECURE=  # true (for port 465) or false (for port 587)
EMAIL_USER=    # Your sending email address (e.g., yourname@gmail.com)
EMAIL_PASS=    # Your email password or app-specific password
ADMIN_EMAIL=   # Email address to receive contact form submissions
